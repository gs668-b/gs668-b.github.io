# AI 客服工作台优化方案

> 面向前端/后端开发的功能规格说明
> 原型参考：`aiworkbenchprototype.html`

---

## 一、工单列表及筛选区

### 1.1 人员指派

| 项 | 说明 |
|---|---|
| 交互 | 工单卡片右上角显示负责人头像，点击弹出指派菜单 |
| 菜单 | 列出全部客服人员，当前指派人显示 ✓，选中即完成指派，无需二次确认 |
| 数据 | 工单对象需增加 `assignee` 字段（用户 ID），后端提供人员列表接口 |

### 1.2 「只看我负责的」开关

| 项 | 说明 |
|---|---|
| 样式 | 滑动开关（toggle） |
| 位置 | 筛选栏右侧 |
| 行为 | 开启后工单列表过滤为 `assignee === 当前用户`，关闭后恢复全部 |

### 1.3 工单时间

| 项 | 说明 |
|---|---|
| 显示 | 每条工单右下角显示「最新一次对话的日期+时间」，格式 `MM-DD HH:mm` |
| 数据 | 工单对象取最新一条消息的 `created_at`，后端按 `last_message_at` 排序 |

---

## 二、回复区

### 2.1 对话区

| 项 | 说明 |
|---|---|
| 自动滚动 | 页面加载后对话区默认滚动到最底部（最新消息），模拟聊天应用体验 |
| 数据 | 每条消息按 `created_at` 升序排列，加载后执行 `scrollTop = scrollHeight` |
| 输入框 | 富文本编辑器，支持图片直接粘贴（无感式，不显示工具栏）；不需要加粗、字体颜色、下划线等格式功能 |
| 对话气泡 | 样式需支持富文本，允许直接展示邮件中的 HTML 内容 |

### 2.2 模板选择器（点击「重新生成」触发）

#### 弹窗结构

居中模态框，顶部平台切换 + 三栏布局：

**顶部平台 Tab**

| Tab | 内容 |
|---|---|
| 🛒 Amazon | 显示 Amazon 平台模板（编码前缀 A-），右侧显示总数 |
| 🌐 官网 Website | 显示 Website 平台模板（编码前缀 W-），右侧显示总数 |

平台总数由 `templateData` 实时统计，非硬编码。

**三栏布局**

| 栏 | 内容 |
|---|---|
| 左栏 | 阶段分类列表（通用 G / 售前 PRE / 售中 MID / 售后 POST），选中 POST 后展开二级子分类（如 POST-01 产品售后问题、POST-02 破损/发错货 等） |
| 中栏 | 当前分类下的模板卡片列表，每张卡片显示：模板 ID、名称、徽章（即用/条件/前置条件 + 渠道） |
| 右栏 | 选中模板的详情：ID、名称、徽章、前置条件、场景描述、模板正文（完整英文内容）、参数表 |

#### 顶部

- 标题「📚 话术模板库」+ 关闭按钮
- 搜索框（按模板名称/关键词过滤中栏列表）

#### 底部

- 左侧显示当前选中模板信息（`未选择模板` / `已选择：A-POST-01-01 引导客户发送视频...`）
- 右侧「取消」+「应用模板」按钮

#### 分类体系

**平台分离**：Amazon 和 Website 各自独立一套模板库，通过顶部 Tab 切换。两平台共享相同的阶段分类（通用 / 售前 / 售中 / 售后），但模板编码、内容、署名不同。

**不设「受阻模板」独立分类**。所有模板按业务归属分配到 G / PRE / MID / POST 分类中。原「受阻」模板在人工调用时不再受限，仅通过 `level` 字段标记为「前置条件」徽章，提示运营注意。

模板 `level` 字段枚举：

| 值 | 徽章文案 | 含义 |
|---|---|---|
| `g` | 通用 | 通用兜底模板，任何场景可用 |
| `ready` | 即用 | 无前置条件，可直接使用 |
| `cond` | 条件 | 需根据客户回复判断是否适用（仅 Amazon 使用，Website 因渠道单一不使用此级别） |
| `prereq` | 前置条件 | AI 自动取用时受前置条件阻断；人工调用时可使用，但需确认前置条件已满足 |

#### 编码规则

| 平台 | 通用 | 售前 | 售中 | 售后 |
|---|---|---|---|---|
| Amazon | A-G1~G4 | A-PRE-01~02 | （无） | A-POST-01-01 ~ A-POST-07-xx（按二级分类编号） |
| Website | W-G1~G4 | W-PRE-01~07 | W-MID-01~10 | W-POST-01~09 |

#### 模板数据结构

```typescript
interface Template {
  id: string;           // 模板编号，如 "A-POST-01-01"、"W-PRE-03"、"A-G1"
  name: string;         // 模板中文名称
  level: 'g' | 'ready' | 'cond' | 'prereq';
  channel: string;      // 渠道限制，如 '全部'、'仅Service邮箱'、'官网'
  prereq: string;       // 前置条件描述（中文）
  scenario: string;     // 适用场景描述（中文）
  content: string;      // 模板英文正文（含 \n 换行）
  params: Param[];      // 参数列表
}

interface Param {
  name: string;         // 参数名，如 '{订单号}'、'[CONFIRM:退款金额]'
  type: string;         // 类型：'AI填充' | '运营确认' | '占位符'
  desc: string;         // 参数说明
}

// 按平台组织的模板数据
interface TemplateData {
  amazon: {
    common: Template[];  // A-G1~G4
    pre: Template[];      // A-PRE-xx
    mid: Template[];     // A-MID-xx
    post: Template[];   // A-POST-xx-xx（按二级分类平铺）
  };
  website: {
    common: Template[];
    pre: Template[];
    mid: Template[];
    post: Template[];
  };
}
```

#### 格式统一规则

- **称呼**：统一用 `Hello,`，不提取客户名称（避免提取错误造成的尴尬；不用 Hi / Dear）
- **结尾**：`Best regards,` + 换行 + 平台署名
  - Amazon：`MelodySusie Support Team`
  - Website：`MelodySusie Customer Service Team`
- **参数标记**：`{xxx}` = AI 从数据源自动填充；`[CONFIRM:xxx]` = 需运营确认后方可填入
- **模板层级**：即刻可用 / 条件 / 前置条件（详见上文 level 枚举）

#### 应用模板

点击「应用模板」后，将模板 `content` 写入回复编辑器，关闭弹窗。

### 2.3 发信功能

点击「📧 发信功能」按钮在回复区展开/收起面板。

| 字段 | 类型 | 说明 |
|---|---|---|
| 发件身份 | 下拉 | 选择发信邮箱身份（service.pro@ / support@ / info@ / marketing@） |
| 收件人 | chip 列表 | 默认填入客户邮箱；直接输入邮箱地址敲回车即可添加新收件人，无需编辑按钮；chip 旁有 × 可删除；含邮箱格式校验 |
| 抄送 CC | 文本输入 | 支持多个邮箱，逗号分隔 |
| 密送 BCC | 文本输入 | 同上 |
| 定时发送 | checkbox + datetime-local | 勾选后选择发送时间，到时自动发送 |

### 2.4 自由发信

点击「✉️ 自由发信」按钮弹出独立模态框。

| 字段 | 说明 |
|---|---|
| 发件身份 | 下拉选择邮箱身份 |
| 收件人 | 自由输入邮箱（chip 样式，回车添加，含格式校验） |
| 抄送 / 密送 | 各一行输入 |
| 主题 | 文本输入 |
| 模板插入 | 调用模板选择器弹窗（与 2.2 共享同一组件），位于底部左侧 |
| 附件 | 位于抄送/密送下方，「📎 添加附件」按钮 |
| 定时发送 | 附件下方，勾选框「启用定时发送」，勾选后展开 datetime-local 时间选择 |
| 底部布局 | 左侧：「插入模板」按钮（左对齐）；右侧：「存为草稿」「取消」「发送」按钮（右对齐） |

### 2.5 回复区工具栏

| 按钮 | 说明 |
|---|---|
| 📧 发信功能 | 展开/收起发信面板（见 2.3） |
| ✉️ 自由发信 | 弹出自由发信模态框（见 2.4） |
| 📎 附件 | 添加附件 |
| 🔄 重新生成 | 点击弹出模板选择器弹窗（见 2.2） |

---

## 三、右侧面板（Tab 切换）

顶部 5 个横向 icon，鼠标悬停显示 tooltip 全称，点击切换内容区。每个 Tab 内容区独立滚动。

### 3.1 回复建议（💡）

四张卡片，从上到下：

| 卡片 | 语义 | 内容 |
|---|---|---|
| 🧠 为什么这样回复 | 默认 | 回复结构的逻辑说明（AI 生成的自然语言） |
| 📚 遵循的模板 | 默认 | 引用了哪些话术模板（模板 ID + 名称），承诺强度说明 |
| ⚡ 建议运营执行 | 警告 | 需要人工核实/决策的动作清单（如「确认地址」「核实保修期」） |
| ⚠️ 历史遗漏提醒 | 危险 | 承诺未兑现（如承诺补发但未发出）、未回复的问题等历史风险 |

### 3.2 客户历史订单信息（📦）

#### 客户画像

| 字段 | 说明 |
|---|---|
| 历史订单数量 | 累计订单数 |
| 总 GMV | 累计消费金额 |
| 平均订单金额 | 总 GMV / 订单数 |
| 最近购买 | 最近一笔订单日期 |

#### 订单列表

Tab 切换：当前售后订单 / 全部订单，位于订单列表标题下方。

**卡片布局**（两行 header）：

| 行 | 左侧（左对齐） | 右侧（右对齐） |
|---|---|---|
| 第一行 | 订单号 + 📋 复制 icon | 金额 |
| 第二行 | 订单日期 | 订单状态 |

header 右侧有展开/折叠 chevron icon，点击整行 header 可切换展开/折叠。

**当前售后订单 Tab**：
- 自动展开详情
- 详情内容：下单时间、物流单号（📋 复制 + 🔍 点击跳转到物流查询 Tab 并自动填充单号）、承运商、商品信息（产品图片 + 产品名 + 价格 × 数量）

**全部订单 Tab**：
- 默认折叠
- 点击展开详情，内容同上
- 非当前售后订单的卡片详情底部有「设为当前售后订单」按钮
- 已是当前售后订单的卡片详情底部显示「✓ 已设为当前售后订单」

#### 跨 Tab 交互

- **复制订单号**：点击 📋 icon 复制到剪贴板，icon 临时变为 ✓ 反馈
- **复制物流单号**：同上
- **物流单号 → 物流查询**：点击 🔍 跳转到物流查询 Tab，自动填入单号和承运商
- **设为当前售后订单**：点击后更新该订单的 `isCurrent` 为 true，其余订单为 false，重新渲染两个 Tab 的卡片

### 3.3 物流查询工具（🚚）

| 字段 | 说明 |
|---|---|
| 运单号 | 文本输入（可粘贴），可从订单详情自动填入 |
| 承运商 | 下拉选择（Amazon Logistics / UPS / FedEx / USPS / 4PX 等），可从订单详情自动填入 |
| 查询结果 | 时间线物流轨迹（时间 + 节点 + 状态） |
| 快捷操作 | 复制单号 / 打开承运商官网查询 |

### 3.4 补发工具（🔄）

> ⏸️ **暂时不做**：等其它优化上线之后再做。

顶部警告提示：「请确认补发信息无误后再提交，补发涉及库存和成本。」

| 字段 | 说明 |
|---|---|
| 补发原因 | 下拉（质量问题补发 / 缺件补发 / 物流丢失补发 / 其他） |
| 补发商品 | 列表，每项：商品名称+SKU、数量、成本价 |
| 仓库选择 | 下拉（中国仓 / 美国FBA仓 / 美国自发仓） |
| 收货地址 | 自动带入客户地址，可编辑 |
| 内部备注 | 文本域（仅内部可见，不对客户展示） |
| 提交 | 提交后生成补发工单，流转至仓库 |

---

## 四、设计原则

1. **话术库平台分离**：Amazon 和 Website 各自独立一套模板库，通过顶部 Tab 切换；编码前缀 A- / W- 区分，署名不同（Support Team / Customer Service Team）
2. **人工调用不受阻**：模板的 `level=prereq` 仅在 AI 自动取用时作为前置条件阻断；人工在模板选择器中可自由选用，仅以徽章提示
3. **承诺强度红线**：草稿中涉及承诺的措辞（退款、补发、换新）需与模板正文一致，不得自行加强
4. **称呼安全**：统一用 `Hello,`，不提取客户名称，避免提取错误造成的尴尬
5. **成本动作提示**：补发工具顶部有警告提示，提醒运营确认后再提交
6. **对话聊天化**：对话区默认滚动到最新消息，模拟聊天应用体验
7. **跨 Tab 联动**：订单详情中的物流单号可一键跳转物流查询 Tab，实现数据联动
